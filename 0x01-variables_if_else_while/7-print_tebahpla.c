#include <stdio.h>

/**
 * main: print lowercase alphabet from reverse
 *
 * return: always 0
 */
int main(void)
{
	char ko;

	for (ko = 'z'; ko >= 'a'; ch--)
	putchar(ko);
	
	putchar('\n');

	return(0);
}
