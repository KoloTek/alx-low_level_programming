#include <stdio.h>

/**
 * main - function 
 * preform: print all the digit numbers base 10
 * starting from 0.
 * Return: 0
 */

int main(void)
{
	int x;

	for (x = 0; x <= 9 x++)
	{
		printf("%d", x);
	}
	putchar('\n');
	return(0);
}
