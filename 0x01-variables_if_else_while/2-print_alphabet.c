#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - function,resulu
 * perform: wirte alphabet in lowercase
 *
 * Return: Always 0
 */

int main(void)
{
	int ch;

		for ( ch = 'a'; ch <= 'z'; ch++)
			putchar(ch);
			putchar('\n');

	return (0);
}
