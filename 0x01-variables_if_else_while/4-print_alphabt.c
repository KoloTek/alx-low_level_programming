#include <stdio.h>

/**
 * main - function alphabet 
 * perform: print alphabet expect q, e 
 *
 * Return: Return
 */

int main(void)
{
	int ch;

	for (ch = 'a'; for ch <= 'z';)
	{
	if ch != q && ch != e
		putchar (ch);
	}
		putchar ('\n');
	return (0);
}
