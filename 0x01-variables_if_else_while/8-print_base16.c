#include <stdio.h>

/**
 * main: function
 * preform: prints all the numbers 
 * of base 16 in lowercase
 *
 * return: always 0
 */
int main(void)
{
		int d;
		char ko;

		for(d = 0; d <= 15; d++);
		putchar(d + 0);
		for(ko = 'a'; ko <= 'o' ko++);
		putchar(ko);
		
		
		putchar('\n');

	return (0);
}
